= form_for(current_user.active_relationships.build, :url => { controller: :follows,action: :create } ) do |f|
  %div= hidden_field_tag :following_id, user.id
  = f.submit "フォローする", class: "button btn btn-default t-content-right-top-recommend-user-list-once__follow-btn", "aria-label" => "Left Align"

