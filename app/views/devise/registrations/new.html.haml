%header.user-header
  = fa_icon('twitter 2x', id: 'users_twitter_icon')
  .login
    アカウントをお持ちの場合 ログイン▽
  .language
    言語: 日本語 ▽
%h2.user-center-text Twitterを使ってみよう
= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
  = devise_error_messages!
  .user-info-field
    = f.label :email
    %br/
    = f.email_field :email, autofocus: true, class: 'info-text-box'
  .user-info-field
    = f.label :password
    - if @minimum_password_length
      %em
        (#{@minimum_password_length} characters minimum)
    %br/
    = f.password_field :password, autocomplete: "off", class: 'info-text-box'
  .user-info-field
    = f.label :password_confirmation
    %br/
    = f.password_field :password_confirmation, autocomplete: "off",class: 'info-text-box'
  .user-info-field
    = f.submit "アカウント作成", class: 'user-info-btn'
